server:
  port: 9090

spring:
  application:
    name: API-GATEWAY
  cloud:
    gateway:
      routes:
        - id: RESTAURANT-SERVICE
          uri: lb://RESTAURANT-SERVICE
          predicates:
            - Path=/restaurants/**
          filters:
           - name: CircuitBreaker
             args:
              name: RESTAURANT-SERVICE
              fallbackuri: /restaurantServiceFallBack
           - name: RequestRateLimiter
             args:
               '[redis-rate-limiter.replenishRate]': 1
               '[redis-rate-limiter.burstCapacity]': 1
        - id: ORDER-SERVICE
          uri: lb://ORDER-SERVICE
          predicates:
            - Path=/orders/**
          filters:
           - name: CircuitBreaker
             args:
              name: ORDER-SERVICE
              fallbackuri: /orderServiceFallBack
           - name: RequestRateLimiter
             args:
               '[redis-rate-limiter.replenishRate]': 1
               '[redis-rate-limiter.burstCapacity]': 1
        - id: PAYMENT-SERVICE
          uri: lb://PAYMENT-SERVICE
          predicates:
            - Path=/payments/**
          filters:
           - name: CircuitBreaker
             args:
              name: PAYMENT-SERVICE
              fallbackuri: /paymentServiceFallBack
           - name: RequestRateLimiter
             args:
               '[redis-rate-limiter.replenishRate]': 1
               '[redis-rate-limiter.burstCapacity]': 1
        - id: DELIVERY-SERVICE
          uri: lb://DELIVERY-SERVICE
          predicates:
            - Path=/deliveries/**
          filters:
           - name: CircuitBreaker
             args:
              name: DELIVERY-SERVICE
              fallbackuri: deliveryServiceFallBack
           - name: RequestRateLimiter
             args:
               '[redis-rate-limiter.replenishRate]': 1
               '[redis-rate-limiter.burstCapacity]': 1
               
okta:
  oauth2:
    issuer: https://dev-53200939.okta.com/oauth2/default
    audience: api://default
    client-id: 0oalokxgzrA44YdvS5d7
    client-secret: P-h-6gsmJhOE2zLlBrDImWo7KRnHI0BotU0Zb7BbTkpV3xk5KDfvnbJTB_5v3usC
    scopes: openid, email, profile, offline_access
        
eureka:
  instance:
    prefer-ip-address: true
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka
      